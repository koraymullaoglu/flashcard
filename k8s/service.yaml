apiVersion: v1
kind: Service
metadata:
  name: flashcard-api
  labels:
    app: flashcard-api
spec:
  type: NodePort
  selector:
    app: flashcard-api
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
